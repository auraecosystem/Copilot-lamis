from engine.schema import create_completion

def generate_completion(model, prompt):
    # Simulated AI response (replace with real AI later)
    response = f"Processed: {prompt}"

    return create_completion(model, prompt, response)
